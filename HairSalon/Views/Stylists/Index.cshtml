@{
    Layout = "_Layout";
}

@using FridayProject.Models;

<h2>Stylists</h2>
@if (Model.Count == 0)
{
    <h3>No Stylists have been added yet!</h3>
}

@foreach (Stylist stylist in Model)
{
    <li>@Html.ActionLink($"{ stylist.Name }", "Details", new { id = stylist.Id })</li>
}

<p>@Html.ActionLink("Add new Stylist", "Create")</p>
